<% title "Category-Items" %>

<div class="col-lg-9">
  <h2 class="buffer-both text-center"><%= @category.name %> <hr /></h2>

  <%= render partial: 'items/category_items' %>

  <%= render partial: 'items/form', locals: {item: @item || @category.items.build} %>
</div>
